apiVersion: v1
kind: Service
metadata:
  name: transaction-api
  namespace: cashflow
spec:
  selector:
    app: transaction-api
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
