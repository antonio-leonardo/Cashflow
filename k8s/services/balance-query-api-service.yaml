apiVersion: v1
kind: Service
metadata:
  name: balance-query-api
  namespace: cashflow
spec:
  selector:
    app: balance-query-api
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
